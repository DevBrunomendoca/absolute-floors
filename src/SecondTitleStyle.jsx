import styled from 'styled-components'

export const ContentSecondTitle = styled.h2`
  font-family: "Cormorant Garamond", serif;
  font-weight: 700;
  color: var(--second-color);
  font-size: 4.5rem;
  text-align: center;
  line-height: 120%;
  //max-width: 600px;
  @media (max-width: 650px) {
    font-size: 4rem;
}
`