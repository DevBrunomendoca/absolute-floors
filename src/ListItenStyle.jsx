import styled from "styled-components"

export const ContainerListItens = styled.ul`
  display: flex;
  align-items: center;
  gap: 1rem;
li{
  list-style: none;
}
a{
  text-decoration: none;
  color: var(--third-color);
}
`